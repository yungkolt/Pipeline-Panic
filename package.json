{
  "name": "pipeline-panic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AZ-400 Ops RPG — a Pokemon-style Azure DevOps study game",
  "homepage": "https://yungkolt.github.io/Pipeline-Panic/",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && node scripts/clean-site.mjs && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  }
}
